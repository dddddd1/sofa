{
  "name": "@harper/core",
  "version": "0.1.0",
  "private": true,
  "description": "框架无关的领域核心：Design 契约、拼装规则、layout、BOM、定价纯函数",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0"
  }
}

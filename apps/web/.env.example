# Harper Cloud · Web 前端环境变量
#
# 目标：整个前端代码不写死任何域名，换域名时只需改环境变量、不改代码/重新构建。
#
# 三种形态（三选一，按部署方式）：
#  1) 同域（推荐，域名无关）：
#      不留 NEXT_PUBLIC_API_BASE，前端始终请求同站 /v1，由 next.config 的
#      API_UPSTREAM 反向代理到后端。以后换域名/后端地址，只改 API_UPSTREAM。
#  2) 跨域直连：
#      NEXT_PUBLIC_API_BASE=<API 域名，如 https://api.example.com/v1>
#  3) 本地开发：
#      NEXT_PUBLIC_API_BASE=http://localhost:3001/v1
#        —— 本地 Web(:3000) 与 API(:3001) 不同端口，用下面的绝对地址（无需 rewrites）。

# 本地开发时取消下一行注释；同域部署时删除本行即可
NEXT_PUBLIC_API_BASE=http://localhost:3001/v1

# 同域部署时的后端 upstream（仅设了它才启用 /v1 反向代理）
# API_UPSTREAM=https://api.example.com

# OG 元数据所用的站点域名（影响 SEO/分享卡片），部署时设置
# NEXT_PUBLIC_SITE_URL=https://your-domain.com